#Detecting Double Letters in Words
def hasDoubleLetters(s): 
    if len(s) < 2:
        return False

    for i in range(0, len(s) - 1):
        if s[i] == s[i + 1]:
            return True
    return False

s = input('Enter any string: ')
print(hasDoubleLetters(s))
